---

## 📄 lessons/12-modules.md
```markdown
# Modules & import

Use `import` to bring external modules into your code. Chaquopy lets you put Python modules in your app.

Example:
```python
import math
print(math.sqrt(16))
