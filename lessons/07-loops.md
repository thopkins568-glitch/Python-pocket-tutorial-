---

## 📄 lessons/07-loops.md
```markdown
# Loops: for & while

Use `for` to iterate over sequences and `while` to repeat until a condition is false.

Example:
```python
for i in range(5):
    print(i)

count = 0
while count < 3:
    print(count)
    count += 1
