---

## 📄 lessons/03-collections-lists.md
```markdown
# Lists (collections)

Lists are ordered, mutable collections. Use indexing to access, and `append()` to add items. You can loop through lists with a `for` loop.

Example:
```python
items = [1, 2, 3]
items.append(4)
for i in items:
    print(i)
