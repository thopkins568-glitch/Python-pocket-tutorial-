---

## 📄 lessons/04-tuples-sets.md
```markdown
# Tuples & Sets

Tuples are ordered, **immutable** collections. Sets are **unordered** collections of unique items.

Example:
```python
t = (1, 2)
s = set([1, 2, 2])
print(t, s)
