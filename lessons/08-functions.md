---

## 📄 lessons/08-functions.md
```markdown
# Functions (def)

Functions let you bundle reusable logic. Use `def`, provide parameters, and return results.

Example:
```python
def add(x, y):
    return x + y
print(add(2, 3))
