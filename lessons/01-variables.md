# Variables & assignment

Variables are names for values in Python. You can assign values to names using `=`. Python is dynamically typed, meaning you don’t declare types — the interpreter figures them out.

Example:
```python
x = 5
name = "Alice"
print(x, name)
